@*
 * Tabs component
 * Tabbed interface with Alpine.js state management
 *@

@import lib/utils.wtz as utils

@struct Tab {
    id: String,
    label: String,
}

@fn apply(
    tabs: &[Tab],
    default_tab: &str,
    content: @(&str),
    class: Option<&str>,
) {
    <div
        x-data=@```json { activeTab: "@default_tab" } ```@
        class="@class.unwrap_or("")"
    >
        @* Tab list *@
        <div class="inline-flex h-9 items-center justify-center rounded-lg bg-muted p-1 text-muted-foreground" role="tablist">
            @for tab in tabs {
                <button
                    type="button"
                    role="tab"
                    :aria-selected="activeTab === '@tab.id'"
                    @click="activeTab = '@tab.id'"
                    :class="activeTab === '@tab.id' ? 'bg-background text-foreground shadow' : ''"
                    class="inline-flex items-center justify-center whitespace-nowrap rounded-md px-3 py-1 text-sm font-medium ring-offset-background transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50"
                >
                    @tab.label
                </button>
            }
        </div>

        @* Tab panels *@
        @for tab in tabs {
            <div
                x-show="activeTab === '@tab.id'"
                x-transition:enter="transition ease-out duration-200"
                x-transition:enter-start="opacity-0"
                x-transition:enter-end="opacity-100"
                role="tabpanel"
                class="mt-2 ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
            >
                @content(&tab.id)
            </div>
        }
    </div>
}

@* Simple tabs with static content *@
@fn simple(
    tabs: &[Tab],
    default_tab: &str,
    panels: @(&str),
) {
    <@apply tabs=@tabs default_tab=@default_tab content=@panels class=None />
}

@* Vertical tabs variant *@
@fn vertical(
    tabs: &[Tab],
    default_tab: &str,
    content: @(&str),
    class: Option<&str>,
) {
    <div
        x-data=@```json { activeTab: "@default_tab" } ```@
        class="@utils::cn(&["flex gap-4", @class.unwrap_or("")])"
    >
        @* Vertical tab list *@
        <div class="flex flex-col space-y-1" role="tablist">
            @for tab in tabs {
                <button
                    type="button"
                    role="tab"
                    :aria-selected="activeTab === '@tab.id'"
                    @click="activeTab = '@tab.id'"
                    :class="activeTab === '@tab.id' ? 'bg-muted' : ''"
                    class="inline-flex items-center whitespace-nowrap rounded-md px-3 py-2 text-sm font-medium ring-offset-background transition-all hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
                >
                    @tab.label
                </button>
            }
        </div>

        @* Tab panels *@
        <div class="flex-1">
            @for tab in tabs {
                <div
                    x-show="activeTab === '@tab.id'"
                    x-transition
                    role="tabpanel"
                >
                    @content(&tab.id)
                </div>
            }
        </div>
    </div>
}
