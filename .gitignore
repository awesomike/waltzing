tree-sitter/node_modules/
node_modules/
extensions/zed/target/
.DS_Store

# Rust build artifacts
/target/

# Generated files (regenerated by build.rs)
# Keep the placeholder but ignore regenerated content
# src/generated/ is tracked but regenerated on build

# Dev-install grammar clone (Zed pulls grammar source here at install time)
extensions/zed/grammars/waltzing/

# Browser test artifacts
/test-results/
/playwright-report/

# Internal-only files — must NOT be published to the public repo.
# (Public repo scope = tree-sitter/, extensions/, libraries/, website, release binaries.)
/.beads/
/.ralph-tui/
/.claude/
/tasks/
/AGENTS.md
/skills.md
/waltzing-expert.md

# Generated codegen output (regenerated by build.rs)
/out/
